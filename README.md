# Premium Cinematic Portfolio

This project is a high-end cinematic portfolio website built with React, Three.js, and Framer Motion.

## ⚠️ Prerequisites

**Node.js is required.**
The automated setup detected that Node.js is missing or not in your PATH. Please install Node.js from [nodejs.org](https://nodejs.org/).

## 🚀 Getting Started

Once Node.js is installed, open a terminal in this directory and run:

1.  **Install Dependencies**
    ```bash
    npm install
    ```

2.  **Start Development Server**
    ```bash
    npm run dev
    ```

3.  **Open in Browser**
    The terminal will show a URL (usually `http://localhost:5173`). Open this link to view the site.

## 🎨 Project Structure

-   `src/components/3d`: Three.js (Canvas) components (ParticleWorld).
-   `src/components/ui`: UI components (Cursor, Timeline).
-   `src/sections`: Page sections (Hero, Reels, Contact).
-   `src/index.css`: Global "Cinematic" styles and variables.

## 🌟 Features Implemented

-   **Particle World**: Interactive 3D background.
-   **Cinematic Cursor**: Custom cursor with lag and trailing glow.
-   **Timeline Scroll**: Scroll progress indicator matching the prompt's "video timeline" concept.
-   **Responsive Sections**: Hero, Reels (Holographic Cards), and Contact Form (Liquid Light).
